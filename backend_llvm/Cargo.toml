[package]
name = "backend_llvm"
version = "0.1.0"
edition = "2024"

[dependencies]
inkwell = { workspace = true }
parser = { path = "../parser" }
type_engine = { path = "../type_engine" }
codegen = { path = "../codegen" }
